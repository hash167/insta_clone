$("#like_<%= @post.id %>").removeClass('glyphicon-heart like').addClass('glyphicon-heart-empty');
$("#likes_<%= @post.id %>").html("<%= j (render partial: 'posts/likes', locals: { post: @post} ) %>");
$("#like_<%= @post.id%>").attr("href", "<%= j (like_post_path(@post.id))");